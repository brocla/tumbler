# Normalize line endings to LF in the repo; check out as LF on all platforms.
* text=auto eol=lf

# Windows-specific binary files — no line ending conversion
*.ico binary
*.png binary
*.icns binary
*.pdf binary
